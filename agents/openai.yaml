interface:
  display_name: "Einstein Skill"
  short_description: "Invent principles through thought experiments"
    default_prompt: "Use $einstein to compare angles, find the most beautiful local-to-global bridge, state a bold conjecture, and stress it before searching prior work."

policy:
  allow_implicit_invocation: true
